import * as Styled from './styles';

export default function Searchbar() {
  const handleSubmit = () => {
    return null;
  };

  return (
    <Styled.Wrapper>
      <Styled.Form action='' onSubmit={handleSubmit}></Styled.Form>
    </Styled.Wrapper>
  );
}
